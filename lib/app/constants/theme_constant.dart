import 'package:flutter/material.dart';

class ThemeConstant {
  ThemeConstant._();
  static const Color darkPrimaryColor = Color(0xFF416FDF);
  static const Color primaryColor = Color(0xFF416FDF);
  static const Color appBarColor = Color(0xFF416FDF);
}
